{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "export $(cat ../../.env | xargs) && export CLOUDFLARE_API_TOKEN=$CLOUDFLARE_API && wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.11.4",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}